class Product {
    constructor(id, ownerId, title, urlImage, description, price) {
        this.id = id;
        this.ownerId = ownerId;
        this.title = title;
        this.urlImage = urlImage;
        this.description = description;
        this.price = price
    }
}

export default Product;